# The Agent service layer drives Metis's coding-agent foundation (pi)
# and translates its native event stream into a canonical UI vocabulary
# (UiEvent).
module Agent
  class Error < StandardError; end
end
