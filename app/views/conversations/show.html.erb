<% content_for :title, @conversation.display_title %>

<%= turbo_stream_from @conversation %>

<div data-controller="chat">
  <header class="mb-6">
    <%= link_to "← Conversations", conversations_path,
          class: "text-sm text-gray-500 hover:text-gray-800" %>
    <h1 class="mt-1 text-xl font-semibold"><%= @conversation.display_title %></h1>
    <div class="mt-2 flex items-center gap-2 text-xs text-gray-400">
      <span class="uppercase tracking-wide"><%= @conversation.backend %></span>
      <%= render "context_meter", conversation: @conversation %>
    </div>
  </header>

  <div id="messages" data-chat-target="messages" class="space-y-4 pb-32">
    <%= render @messages %>
  </div>

  <%= render "composer", conversation: @conversation %>
</div>
