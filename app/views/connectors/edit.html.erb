<% content_for :title, @connector.name %>

<main class="pane">
  <header class="pane-head">
    <%= link_to connectors_path, class: "ico-btn", title: "Back to connectors" do %>
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
        <path d="M10 3L5 8l5 5" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    <% end %>
    <h1 class="pane-title"><%= @connector.name %></h1>
    <div style="flex:1"></div>
    <%= button_to "Remove", connector_path(@connector), method: :delete, class: "btn btn-danger",
          form: { data: { turbo_confirm: "Remove this connector?" } } %>
  </header>

  <div class="pane-body">
    <div class="pane-inner">
      <%= render "form", connector: @connector %>
    </div>
  </div>
</main>
