<% if previewer.markdown? %>
  <article class="preview-markdown chat-content">
    <%= markdown(previewer.full_content) %>
  </article>
<% else %>
  <pre class="preview-text"><%= previewer.full_content %></pre>
<% end %>
