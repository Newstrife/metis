<% rows = previewer.all_rows %>
<table class="preview-csv">
  <% rows.each_with_index do |row, idx| %>
    <% tag = idx.zero? ? "th" : "td" %>
    <tr>
      <% row.each do |cell| %>
        <<%= tag %>><%= cell %></<%= tag %>>
      <% end %>
    </tr>
  <% end %>
</table>
